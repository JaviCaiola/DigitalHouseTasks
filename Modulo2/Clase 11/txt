En esta clase se vio dos formas de manipular datos en JavaScript

-Destructuring
-Spread operator

# DESTRUCTURING
Se encarga de extraer los valores de un array o un objeto y copiarlos
en otras variables sin necesidad de tener que acceder a los indices
la estructuracion se puede ver en el archico ./app.js en esta carpeta

 En el caso de las arrays, los valores que se asignaran en las variables
que declaremos dentro de los [] se addicionaran por el orden del array es
decir de 0 en adelante; para omitir valores especificos se puede dejar un
espacio en blanco entre las variables ej: [var1, , var2, , , ,var4] y asi.

 Para los objetos literales se asignan las variables entre {} y estas
variables tienen que tener el mismo nombre de la propiedad que hay dentro.

# Spread operator y rest parameter
 Son dos herramientas 

-spread operator (operador de propagación)
-rest parameter (parámetro rest)

 El Spread Operator funciona para fusionar tanto listas como objetos sin
la necesidad de tener que llamar a los metodos de los mismos. tan solo
agregando uno al otro con "..." seguido del nombre de la otra lista o 
objeto que queramos pasarle

 El rest parameter tiene la misma sintaxis de los tres puntos, a diferencia
que este va como parametro en los parentesis de una funcion, su objetivo es
que a la funcion que se los pasemos pueda recibir cualquier cantidad de 
parametros sin que se los tengamos que asignar. ej:
 funtion hacerCosa(...parametro){}

hacerCosa(valor1, valor2, valor3, valor4); y asi



